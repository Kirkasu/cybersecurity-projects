[Failed Logon Brute-Force → Success (10m)]
search = index=main source="WinEventLog:Security" (EventCode=4625 OR EventCode=4624) earliest=-10m ...
cron_schedule = */5 * * * *
alert_type = always
dispatch.earliest_time = -10m
dispatch.latest_time = now

[Encoded PowerShell Detected (10m)]
search = index=main source="WinEventLog:Microsoft-Windows-Sysmon/Operational" EventCode=1 earliest=-10m ...
cron_schedule = */5 * * * *
alert_type = always

[Outbound Connection Spike (10m)]
search = index=main source="WinEventLog:Microsoft-Windows-Sysmon/Operational" EventCode=3 earliest=-15m ...
cron_schedule = */5 * * * *
alert_type = always
